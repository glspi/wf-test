name: test1

on:
  push:
    branches:
      - main

jobs:
  hello:
    # runs-on: [ubuntu-latest, windows-latest]
    runs-on: [ubuntu-latest, ubuntu-18.04]
    steps:
      - name: yolo
        run: echo "hi hi!"
  # build:
  #   runs-on: [ubuntu-latest, windows-latest]